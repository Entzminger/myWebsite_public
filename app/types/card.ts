/**
 * Data model for the `Card` component.
 */

/**
 * The element a card stands as: `article` for a card that stands on its own,
 * `form` for the contact form, `section` for a named block inside a section.
 */
export type CardElement = 'article' | 'form' | 'section';
