/**
 * The jump-to-top button. It knows only the first section of the page - its
 * id is both the jump target and the marker it becomes visible from.
 */

/** Jump target: the first section of the page. */
export const backToTopTarget = 'profile';

/**
 * The button shows nothing but the arrow, so its accessible name stands here
 * and names the destination rather than just the direction.
 */
export const backToTopLabel = 'Nach oben zu „Über mich“ springen';

